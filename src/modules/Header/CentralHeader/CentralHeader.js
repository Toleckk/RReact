import React from "react";

const CentralHeader = () =>
    <div id={'central-header'} style={{color: 'white'}}>
        <span style={{position: 'relative', left: 50 + '%'}}>SAS</span>
    </div>;

export default CentralHeader;